# The URL the site will be built for
base_url = "https://tech.dreamleaves.org"

# The site title and description; used in feeds by default.
title = "Red Leaves"
description = "My whole life was unlimited red code."
author = "Arnaud 'red' Rouyer"

# The default language; used in feeds.
default_language = "en"

# The site theme to use.
# theme = "hyde"

taxonomies = [
  {name = "categories", feed = true},
  {name = "tags", feed = true},
  {name = "languages", feed = true}
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

generate_feeds = true
feed_filenames = ["atom.xml"]
output_dir = "_site"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true


[extra] # hyde
# Put all your custom variables here
hyde_theme = "theme-base-08"
hyde_sticky = false
hyde_links = [
  { url = "/", name = "Technical blog" },
  { url = "/languages", name = "Languages" },
  { url = "/about", name = "About red" }
]
